###  python3 zip(）的用法

# 1. 基本操作。按照顺序对多个可迭代对象打包。
a = [1, 2, 3]
b = [4, 5, 6]
T = list(zip(a, b))
print(T)
# [(1, 4), (2, 5), (3, 6)]

# 2.  zip(*)，是解压。
g = zip(*T)
print(list(g))
# [(1, 2, 3), (4, 5, 6)]


